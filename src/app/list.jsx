const React = require('react');
const ReactDOM = require('react-dom');

import { Button } from 'antd-mobile';


import styles from './../app.less';

export default class List extends React.Component{
	render(){
        return(
            <div className="list">
                <Button>start666666666</Button>
            </div>
        )
    }
}
